#include <stdio.h>

/**
 * main - Entry point of the program.
 *
 * @argc: Number of command line arguments.
 * @argv: Array containing the program command line arugments.
 *
 * Return: 0 - Success
 */
int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
